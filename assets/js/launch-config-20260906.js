// Enable only after the receiving API, retention job and consent text are approved.
// Only a public Turnstile site key belongs here. Never add server credentials.
window.USELESS_LAUNCH_CONFIG = Object.freeze({
  endpoint: '',
  turnstileSiteKey: '',
  consentVersion: '20260906-v2-phone'
});
